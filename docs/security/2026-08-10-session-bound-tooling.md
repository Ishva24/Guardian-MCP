# Session-Bound Tooling for Guardian-MCP

Session-bound MCP tooling constrains a valid token to the resources and environments it was issued for. This reduces blast radius when a prompt tries to redirect a tool call toward another project, file prefix, or deployment environment, and gives audit logs a precise reason when a request is denied.
